# Sample inputs

Drop `e1s1.csv` here (the canonical example). Once its exact format is known,
`docs/01-csv-input-format.md` will be finalized and a golden test added that renders
`e1s1.csv` -> `outputs/e1s1_test.bmp`.
