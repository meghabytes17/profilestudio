# Core runtime dependencies for the Incoming Profile Utility.
# These match the original tool's stack (reference/profile_sketcher.py).

numpy>=1.26          # arrays, scaling, coordinate math
pandas>=2.0          # CSV (width/height trace) parsing
opencv-python>=4.9   # polygon fill + native .bmp write (use -headless on CI/servers)
customtkinter>=5.2   # themed tkinter GUI (file pickers, nm/px field, Generate)
pillow>=10.0         # image handling / Tk preview interop

# Recommended for the parametric profile builders (docs/03): polygon offsetting
# gives conformal films, tapered/re-entrant walls, footing, etc. for near-free.
shapely>=2.0

# Dev/test:
#   pytest>=8.0
