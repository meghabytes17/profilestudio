"""Graphical user interface.

The original tool (reference/profile_sketcher.py) is built with **customtkinter**
(a themed tkinter). We keep that: it already provides file pickers, an editable
nm/px field, and a Generate button. This module will grow into a refactor of that
UI plus new parameter controls for the parametric profile builders (docs/03).
"""
from __future__ import annotations


def launch() -> None:
    """Launch the customtkinter GUI. TODO: refactor from reference/profile_sketcher.py."""
    raise NotImplementedError("GUI refactor pending — see reference/profile_sketcher.py.")
