"""Graphical user interface.

Default framework: tkinter (stdlib). If a richer live preview is needed, switch to
PySide6 (optional dependency `gui-qt`). See README "Key design decisions".

Intended widgets: CSV file picker, parameter fields (pitch, thicknesses, SWA, ...),
a preview pane, and a "Save .bmp" action.
"""
from __future__ import annotations


def launch() -> None:
    """Launch the GUI. TODO: build with tkinter (or PySide6)."""
    raise NotImplementedError
